<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>isPortal__c</fullName>
    <formula>IF( MONTH(TODAY())&gt;2 , 
IF( YEAR(CloseDate)=YEAR(TODAY()) || YEAR(CloseDate)=YEAR(TODAY())+1 , true, false) , 
IF( YEAR(CloseDate ) &gt;= YEAR(TODAY())-1, true, false) )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>isPortal</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
